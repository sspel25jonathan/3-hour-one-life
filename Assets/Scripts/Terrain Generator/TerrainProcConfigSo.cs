using UnityEngine;



[CreateAssetMenu(fileName = "TerrainProcConfig", menuName = "ScriptableObjects/TerrainProcConfig", order = -1)]
public class TerrainProcConfigSo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
}
